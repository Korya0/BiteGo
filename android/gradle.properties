org.gradle.jvmargs=-Xmx3072m -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
# Limit parallel workers: the default spawned 140+ threads and exhausted native memory alongside the emulator.
org.gradle.workers.max=2
org.gradle.parallel=false
# Keep the Kotlin compile daemon lean so it doesn't double the JVM footprint.
kotlin.daemon.jvmargs=-Xmx1536m
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
